# Core (naive baseline runs with just these)
anthropic>=0.40
psycopg[binary]>=3.2
pgvector>=0.3
fastembed>=0.3
python-dotenv>=1.0

# Optional / later tiers (uncomment as you reach them):
# openai>=1.40           # TASK 3: hosted embeddings (alt provider)
# cohere>=5.0            # TASK 4b: reranking (Cohere Rerank)
# ragas>=0.2             # TASK 10: standard RAG eval metrics
# langfuse>=2.0          # TASK 11: tracing/observability
